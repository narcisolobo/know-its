import { Rubik, Shantell_Sans } from 'next/font/google';

const rubik = Rubik({
  subsets: ['latin'],
  display: 'swap',
});

const shantell = Shantell_Sans({
  subsets: ['latin'],
  display: 'swap',
});

export { rubik, shantell };
